export def reverse(a) {
    store i = 0;
    store j = len(a) - 1;
    while (i < j) {
        store tmp = a[i];
        a[i] = a[j];
        a[j] = tmp;
        i = i + 1;
        j = j - 1;
    }
    return a;
}