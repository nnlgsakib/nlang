export def pow(base: int, exp: int) -> int {
    if (exp < 0) { return 0; }
    store result = 1;
    for (store i = 0; i < exp; i = i + 1) {
        result = result * base;
    }
    return result;
}